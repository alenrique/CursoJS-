function verificar() {
    var tab = document.getElementById("seltab")
    var num = document.getElementById("num")
    n = num.value
    
    if (n < 0) {
        window.alert("[ERRO] Tente com um numero positivo")
    }else {
        for (let i = 0; i < n; i++) {
            
        }
    }
}